num_list = []
sum_numbers = 0
for i in range(5):
    n = int(input())
    num_list.append(n)
    sum_numbers += n

print(num_list)

#Create two lists and append all the elements
# Display square of each element in the list
sq_list = [x for i in range(5): sq_list.append(int(input())*int(input()))]
# Find 2nd max number among the list
# Arrange all the element in sorted order